venv/
__pycache__/
*.pyc

# Raw CIC-IDS2017 CSVs (1.25 GB+) and processed splits must never be committed
data/
Data/

# Binary model artifacts - regenerated by the pipeline
models/*.pkl
models/*.npy

# Whitelist the artifacts the runtime app needs (force-added to git)
!models/best_model.pkl
!models/scaler.pkl
!models/label_encoder.pkl

# Persisted app state (SQLite) must never be committed
# Local state & demo artifacts
*.db
*.sqlite
demo/pcaps/

# Report / academic documents stay out of the repo
*.docx

# Scratch / OS files
.ipynb_checkpoints/
.DS_Store